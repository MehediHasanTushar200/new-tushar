@model hamko.ViewModels.StockInSearchViewModel

@{
    ViewBag.Title = "Stock In Search";
}

<h2>Stock In List</h2>

@if (Model.Results != null && Model.Results.Any())
{
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>Product</th>
                <th>Total Quantity</th>
                <th>Unit Price (Avg)</th>
                <th>Total Price</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.Results)
            {
                <tr>
                    <td>@item.Name</td>
                    <td>@item.Quantity</td>
                    <td>@item.Price.ToString("N2")</td>
                    <td>@item.TotalPrice.ToString("N2")</td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <p>No stock data found.</p>
}

@* @if (Model.TotalPages > 1) *@
@* { *@
@*     <nav class="mt-3 d-flex justify-content-center"> *@
@*         <ul class="pagination"> *@
@*             <li class="page-item @(Model.PageNumber == 1 ? "disabled" : "")"> *@
@*                 <a class="page-link" asp-action="StockInList" asp-route-page="@(Model.PageNumber - 1)">Previous</a> *@
@*             </li> *@

@*             @for (int i = 1; i <= Model.TotalPages; i++) *@
@*             { *@
@*                 <li class="page-item @(i == Model.PageNumber ? "active" : "")"> *@
@*                     <a class="page-link" asp-action="StockInList" asp-route-page="@i">@i</a> *@
@*                 </li> *@
@*             } *@

@*             <li class="page-item @(Model.PageNumber == Model.TotalPages ? "disabled" : "")"> *@
@*                 <a class="page-link" asp-action="StockInList" asp-route-page="@(Model.PageNumber + 1)">Next</a> *@
@*             </li> *@
@*         </ul> *@
@*     </nav> *@
@* } *@
